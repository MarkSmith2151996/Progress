# ===========================================
# Progress Tracker - Environment Variables
# ===========================================

# PIN Protection (required for Vercel deployment)
# Set a 4-6 digit PIN to protect your app
ACCESS_PIN=1234

# Google Sheets Storage (required for Vercel)
# Get these from Google Cloud Console
GOOGLE_SPREADSHEET_ID=your-spreadsheet-id-here
GOOGLE_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# ===========================================
# Optional (for local development only)
# ===========================================

# Anthropic API Key (only needed if using Claude API directly)
# ANTHROPIC_API_KEY=sk-ant-...

# Data directory for local JSON storage (defaults to ~/.progress-tracker-data)
# PROGRESS_TRACKER_DATA_DIR=/path/to/data
